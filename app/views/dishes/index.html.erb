<p id="notice"><%= notice %></p>

<h1 align="center">Dishes</h1>


  <div class="container-fluid">
    <% @dishes.each do |dish| %>
      <%=render "dishes/showdishes", dish: dish, chef: dish.chef, divide: " - "  %>
    <% end %>
  </div>
<!--
<table>
  <thead>
    <tr>
      <th>Chef</th>
      <th>Name</th>
      <th>Description</th>
      <th>Picture</th>
      <th>Score</th>
      <th>Price</th>
      <th>Calories</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dishes.each do |dish| %>
      <tr>
        <td><%= dish.chef %></td>
        <td><%= dish.name %></td>
        <td><%= dish.description %></td>
        <td><%= dish.picture %></td>
        <td><%= dish.score %></td>
        <td><%= dish.price %></td>
        <td><%= dish.calories %></td>
        <td><%= link_to 'Show', dish %></td>
        <td><%= link_to 'Edit', edit_dish_path(dish) if policy(dish).edit? %></td>
        <td><%= link_to 'Destroy', dish, method: :delete, data: { confirm: 'Are you sure?' } if policy(dish).destroy? %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Dish', new_dish_path %> -->
