<%= form_for(order_allergy) do |f| %>
  <% if order_allergy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_allergy.errors.count, "error") %> prohibited this order_allergy from being saved:</h2>

      <ul>
      <% order_allergy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order_id %>
    <%= f.text_field :order_id %>
  </div>

  <div class="field">
    <%= f.label :allergie_id %>
    <%= f.text_field :allergie_id %>
  </div>

  <div class="actions" align="center">
    <%= f.submit :class => "btn btn-embossed btn-primary" %>
  </div>
<% end %>
