<%= bootstrap_form_for [order] do |f| %>

  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <h3> <%= order.offer.dish.name %> </h3>
    <h5> <%= order.offer.dish.chef.username %> </h5>
  </div>

  <%= f.hidden_field :offer_id, {:value => params[:offer_id]} %>
  <%= f.hidden_field :user_id, {:value => current_user.id} %>

  <div class="field">
    <%= f.select :amount, (1 .. order.offer.amount) %>
  </div>

  <div class="field">
    <%= f.datetime_select :date, start_year: Date.today.year, end_year: Date.today.year + 1  %>
  </div>

  <div class="field">
    <%= f.text_area :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>